#include <stdio.h>

int main()
{
    int i;
    char palavra[i];

    while (palavra[i]<=i)
   {
    printf("Digite uma palavra:");
    fgets(palavra, i,stdin);
   }

    printf("Determine uma quantidade de vezes para escrever uma palavrar: ");
    scanf("%d",i);

    
    return 0;
}
